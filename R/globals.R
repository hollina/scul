#' @import utils
if(getRversion() >= "2.15.1")
  utils::globalVariables(c("SCUL.input", "SCUL.inference", "SCUL.output",
                         "group", "posttreat",
                         "std_diff", "value",
                         "MSE", "vals", "ltype",
                         "lcols", "SE", "Ref",
                         "cvPLOT"))
